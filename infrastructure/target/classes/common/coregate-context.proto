syntax = "proto3";

package br.com.coregate.infrastructure;

option java_multiple_files = true;
option java_package = "br.com.coregate.proto.transaction";
option java_outer_classname = "CoreGateContextProto";


// =======================================================
// 🌐 Contexto compartilhado
// =======================================================
message CoreGateContext {
    string trace_id = 1;     // Correlation ID (UUID)
    string channel_id = 2;   // Canal lógico / conexão Netty
    string tenant_id = 3;    // Identificação multi-tenant
}